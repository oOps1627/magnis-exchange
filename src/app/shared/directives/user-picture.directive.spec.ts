import { UserPictureDirective } from './user-picture.directive';

describe('UserPictureDirective', () => {
  it('should create an instance', () => {
    const directive = new UserPictureDirective();
    expect(directive).toBeTruthy();
  });
});
